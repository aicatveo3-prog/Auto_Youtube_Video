# 정리본(clean.md) 추가 방법 — 외부 AI/사람용

이 저장소의 자막을 읽고 **정리본**을 만들어 넣는 방법입니다. 여기에 파일을
커밋하면 GitHub Actions가 자동으로 사이트를 다시 빌드해 반영합니다. 로컬 PC는
필요 없습니다.

## 1. 원본 자막 읽기

각 영상의 자막은 아래 경로에 있습니다.

```
transcripts/<VIDEO_ID>/plain.txt     ← 정리할 원본 (문어체 대상)
transcripts/<VIDEO_ID>/meta.json     ← 제목, 채널, 단어 수 등
```

`<VIDEO_ID>` 는 유튜브 영상 ID입니다 (예: `dekLqwB2les`).

## 2. 정리 지침

정리 규칙은 `prompts/cleanup.md` 에 있습니다. 그 지침을 그대로 따르세요.
(구어체→문어체, 반복 제거, 소제목 구조화, 원본 대비 60~70% 분량 등)

## 3. 정리본 저장 위치와 형식

결과를 **같은 폴더**에 `clean.md` 로 저장합니다.

```
transcripts/<VIDEO_ID>/clean.md
```

파일 맨 위에 아래 머리말(front matter)을 넣어주세요. 없어도 표시는 되지만,
넣으면 사이트가 출처/날짜를 함께 보여줍니다.

```markdown
---
source: <VIDEO_ID>
prompt: cleanup
generated: YYYY-MM-DD
---

(여기부터 정리본 본문. ##, ###, **볼드**, > 인용, 표, 🧠 요약 등 마크다운 사용)
```

## 4. 커밋하면 끝

`transcripts/<VIDEO_ID>/clean.md` 를 커밋/푸시하면:

1. GitHub Actions(`.github/workflows/build-pages.yml`)가 자동 실행되고
2. `scripts/build_site.py` 가 `docs/` 를 다시 빌드하고
3. 그 결과를 되돌려 커밋해서
4. 1~2분 뒤 Pages 사이트에 정리본이 나타납니다.

`docs/` 폴더는 자동 생성물이므로 **직접 수정하지 마세요.** 원본은 항상
`transcripts/<id>/clean.md` 입니다.

## 주의

- `raw.json3` 는 저장소에 없습니다(용량 문제로 제외). 정리는 `plain.txt` 로 하세요.
- 원본에 없는 내용을 지어내지 말고, 수치·고유명사·지역명은 정확히 보존하세요.

---

# (선택) 화면 캡쳐 지정 — shots.json

**특정 채널(예: 윈카 AI)** 의 영상에만 적용합니다. 대상 채널 목록은
`config/capture_channels.json` 에 있습니다. 그 목록에 없는 채널은 캡쳐를
지정하지 마세요.

## 원리 (중요)

너(온라인 AI)는 유튜브에 접속할 수 없으므로 **이미지를 직접 뽑지 못합니다.**
너는 "몇 초의 화면을 캡쳐하면 좋은지" 목록만 만들어 커밋합니다. 실제 캡쳐는
**사용자 로컬 PC**에서 `capture.bat` 을 돌릴 때 yt-dlp + ffmpeg 이 수행합니다.
즉 너의 역할은 **판단(시점 지정)**, 로컬의 역할은 **실행(프레임 추출)** 입니다.

## 1. 시간이 붙은 자막 읽기

정리본은 `plain.txt`(시간 없음)로 만들지만, 캡쳐 시점을 찍으려면 **시간이 붙은**
자막이 필요합니다. 아래 파일을 읽으세요.

```
transcripts/<VIDEO_ID>/timed.tsv
```

각 줄 형식은 `<밀리초>\t<초>\t<자막 텍스트>` 입니다. 가운데 **초(seconds)**
값을 보고 캡쳐할 시점을 고르면 됩니다.

## 2. 어떤 순간을 고를까

- 화자가 화면의 자료를 가리키는 대목 (예: "이 그래프를 보시면", "이 표에서",
  "화면에 보이듯이", "빨간 선이", "자료를 보면")
- 정리본의 각 `##` 소제목이 다루는 핵심 장면
- 수치·통계·비교가 화면에 제시되는 순간

의미 없는 장면(인트로, 인사, 화면 전환 없는 구간)은 피하세요.

## 3. shots.json 저장 위치와 형식

정리본과 **같은 폴더**에 `shots.json` 으로 저장합니다.

```
transcripts/<VIDEO_ID>/shots.json
```

형식:

```json
{
  "source": "<VIDEO_ID>",
  "shots": [
    { "t": "3:12", "label": "강남 아파트 가격 그래프" },
    { "t": "8:40", "label": "전월세 통계 표" }
  ]
}
```

- `t` : 시각. `MM:SS` 또는 `HH:MM:SS` (초만 써도 됨). timed.tsv 의 초 값 기준.
- `label` : 그 장면이 무엇인지 짧은 설명. 사이트 캡쳐 갤러리의 캡션이 됩니다.
- 한 영상당 **최대 50개**. 그 이상은 로컬에서 앞의 50개만 처리합니다.

## 4. 커밋한 뒤

`clean.md` 와 `shots.json` 을 함께 커밋/푸시하면 됩니다. 단, **캡쳐 이미지는
바로 나타나지 않습니다.** 사용자가 로컬 PC에서 `capture.bat` 을 실행하면 그때
프레임이 추출되어 저장소에 올라가고, 사이트의 읽기 화면에 캡쳐 갤러리로
표시됩니다.

> 요약: 너는 `shots.json`(시점 목록)만 만들면 되고, 이미지 추출·업로드는 로컬이
> 담당합니다.
